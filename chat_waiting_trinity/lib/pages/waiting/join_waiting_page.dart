import 'package:flutter/material.dart';

class JoinWaitingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Join Wainting List'),
      ),
      body: Text('Join Wainting List')
    );
  }
}